# Node中的模块查找策略

```js
require('./a')

require('../')

1. 以./或者../开头的，就去找本地文件或者本地文件夹

2. 先找文件，如果没写后缀名会自动补全后缀名（js / json），文件没找到，就找文件夹，一般是文件夹里面的index.js文件（如果有配置的话，先根据配置来）， 

require('fs')

3. 如果是这样写的话，首先查看是不是内置模块， 不是的话，看是否是第三方模块

4. 如果是第三方模块，会从node_modules中去查找，依附于第一点和第二点逻辑
```

