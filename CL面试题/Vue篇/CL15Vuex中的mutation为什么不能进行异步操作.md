# vuex中的mutation为什么不能进行异步操作

1. 我们都知道在Vuex中想直接改变状态中的数据的唯一途径就是提交mutation，每一个mutation执行完之后都会得到一个新的状态，这样devtools就可以打个快照存下来，所以如果mutation支持异步的话，就没有办法追踪到状态是何时更新的，不利于调试。
2. 如果想要异步操作的话，需要借助Action来提交mutation，Action充当了异步操作的调度中心，通过commit mutation来实现状态的同步更新。这样可以更方便的跟踪每一个状态的变化

