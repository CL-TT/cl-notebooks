# 文档声明

1. h5版本的文档声明

```html
<!DOCTYPE HTML>
```

  

2. h5版本的文档声明，要比其他版本的更短更简洁，这是为什么呢？

   ```js
   1. html5不需要引入DTD文件，而其他类型的文档声明是需要引入DTD
   
   2. 在早期的HTML版本中，例如html4.01中，文档声明之所以要引入DTD，是因为h4是基于SGML的， 而DTD规定的是标记语言的规则，这样浏览器才能正确的呈现内容
   
   3. 最新版本的html5是不基于SGML的，所以不需要引入DTD
   ```

   